spec = 1
version = "0.1.0"
name = "CLI Smoke Assistant"
summary = "Verify local file-backed Power execution."
creators = ["@roxygens"]
github = "https://github.com/TheShimpz/shimpz-cli"
allowed_hosts = []
genesis = "Return a deterministic greeting for CLI verification."

[accounts.cloudflare]
scopes = ["zone.read"]
