[shimpz]
spec = 1
id = "cli-smoke"
version = "0.1.0"
name = "CLI Smoke Assistant"
summary = "Verify local file-backed Action execution."
creators = ["@roxygens"]
github = "https://github.com/TheShimpz/shimpz-cli"
genesis = "Return a deterministic greeting for CLI verification."

[network]
allowed_hosts = []

[integrations.cloudflare]
scopes = ["zone.read"]
